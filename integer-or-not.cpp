#include <iostream>
using namespace std;
int main(){
      cout<<"enter the number: ";
      float number;
      cin>>number;
     if (float(number) == int(number)) {
           cout<<"is integer";
          
     }
      else cout<<"not integer";
}
