#include <iostream>
using namespace std;
int main(){
      // Calculate Area & Perimeter of Rectangle:
      int length,breath;
      cout<<"enter length: ";
      cin>>length;
      cout<<"enter breath: ";
      cin>>breath;
      cout<<"area of Rectangle: "<<length*breath<<endl;
      cout<<"parameter of Rectangle: "<<2*(length+breath);
      
}
