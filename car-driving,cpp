#include <iostream>
using namespace std;
int main(){
      cout<<"enter your age: ";
      int age;
      cin>>age; 
      if (age >= 18) {
            cout<<"eligible";
      }
      else cout<<"not eligible";
}
